{
  "license": "Apache-2.0",
  "scripts": {
    "predev": "node -e \"try { fetch('http://localhost:5174').then(r => r.text()).catch(() => {}); } catch(e) {}\"",
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "dev:workbench": "cd frontend && npm run dev:workbench",
    "build:workbench": "cd frontend && npm run build:workbench",
    "publish:npm": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"& 'npm-desktop/publish.ps1' @args\""
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1"
  },
  "dependencies": {
    "@tauri-apps/plugin-process": "^2.3.1",
    "@tauri-apps/plugin-updater": "^2.11.0"
  }
}
