target/
*.log
*.pdb
# 仅限根目录：无前缀写法会连 relay-server/.cargo/config.toml 一起吞掉
# （那是 musl 交叉编译的链接器配置，必须入库）
/config.toml
config/team.toml
debug_*
.nuphus/
.nuphus-*
node_modules/
desktop/tesseract/
scripts/__pycache__/
__pycache__/
temp/
frontend/dist/
frontend/dist-workbench/
*.bmp
# 根目录临时 Python 脚本（AI 会话产物，不入库）；scripts/ 正式脚本（generate_icons.py 等）不受影响
/*.py
src-tauri/desktop/models/*.onnx
src-tauri/desktop/models/*.txt
src-tauri/desktop/models/*.pt

# 备份文件
*.bak

# 临时文件
*.tmp

# 编辑器/系统文件（跨平台）
Thumbs.db
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/

# 根目录临时运维脚本（AI 会话产物，不入库）
/*.ps1

# 前端设计预览稿（AI 会话产物，不入库）
/preview-*.html

# 内部审计文件（不公开）
audit-*.md
# 前端内部设计文档（不公开，与 docs/ 同策略）
frontend/docs/
# 源码内部设计文档（不公开）
src/memory/agent-memory-design.md
# 审计报告目录（内部文档，不公开）
audit/
# 内部文档（开发/审计/路线图/计划，均不公开推送；开源壳子仅保留根 README）
# 插件市场建好后会重写公开开发文档
docs/*
!docs/readme-hero/
!docs/readme-hero/*

# 个人工作流数据（不公开）
plugin/workflows/a1b2c3d4*/
plugin/workflows/wechat*/
# 本机媒体应用播放工作流（含本机 exe 绝对路径、屏幕分辨率与操作截图）
plugin/workflows/qqmusic-play-song*/

# 工作流运行快照（保留 workflow.json / params.json / guide.md）
plugin/workflows/*/workflow.2*.json
plugin/ui-maps/im/
plugin/ui-maps/terminal/
# 本机媒体应用 UI 地图（坐标系绑定本机分辨率）
plugin/ui-maps/media/

# 个人 ChatAgent 配置（不公开）
plugin/chat-agents/

# 第三方安装的 skill（不提交）
plugin/skills/community/

# 运行时攒出来的经验条目（工作流执行过程中自动落盘，非人手写；随包只读资产只有
# plugin/experiences/desktop/，见 src/build.rs 的 allowlist）
plugin/experiences/workflow/

# 内部开发计划（含个人路径，不公开）
# TEMPLATE.plan.md 是公开模板，给开源贡献者引导用；具体实例本地化
src-tauri/tasks/nuphus/
src-tauri/tasks/*/plans/

# Agent 自我认知文档（内部设计，不公开；运行时随安装包分发）
plugin/knowledge/nuphus-self/
src-tauri/tasks/*/archive/
!src-tauri/tasks/TEMPLATE.plan.md

# 系统元数据
Thumbs.db
.DS_Store

# 本地构建产物
*.exe
*.dll
*.zip
# IDE
.vscode/
.idea/
*.swp
*.swo

# 环境变量（防 API Key 泄漏）
.env
.env.*

# TypeScript 增量编译
*.tsbuildinfo
# STT 原型验证遗留物（模型包/wav/onnx 库，721MB，不提交）
tools/stt-proto/
# nuphus-mcp 已独立迁出到 C:\Users\Administrator\nuphus-mcp（不再随主仓库）
release/
# 运行时安装的插件应用与实例数据（用户数据，不入库）
plugin/apps/
# 插件市场实现目录（仅本地开发；开源基础版不含市场，动态加载零匹配降级）
frontend/src/market/
# 插件市场体系 UI（2026-08-21 大王决策：市场体系不开源，仅本地开发编辑；
# 入口仅展示筹备提示 PluginComingSoon；市场 ready 后解除忽略并恢复 App.tsx 挂载）
frontend/src/main-window/pages/PluginAppsPage.tsx
frontend/src/main-window/pages/PluginDevPage.tsx
frontend/src/main-window/lib/market-types.ts
frontend/src/main-window/lib/plugin-categories.ts
frontend/src/styles/plugin-dev.css
# 运行时创建的工作流实例（UUID 目录；命名目录为策划工作流，需入库）
plugin/workflows/????????-????-????-????-????????????/


# ── 开源壳子排除（2026-08-19 大王确认：历史压成单提交，做干净壳子）──
# 赞助收款码（商业敏感资产）
relay-server/src/sponsor_wechat_qr.png
relay-server/src/sponsor_wechat_qr.b64
# 本地 Agent 登记（含本机路径/账号信息）
plugin/team.toml
# 本地验证工作流（含真实窗口快照/截图/运行轨迹，非产品示例）
plugin/workflows/chatagent-opencode-verify/
plugin/workflows/nuphus-tour-v4/
# 本地工作流注册表（UUID 实例，用户数据）
plugin/workflows/index.json
# 内部自我认知知识库（含安全架构/中继安全，不公开）
plugin/knowledge/nuphus-self/
# 市场仓聚合脚本（供独立 nuphus-market 仓使用，开源版不含市场）
scripts/build-market-index.mjs


*.log.err
# ── sherpa-onnx 预编译库入库（Windows 构建不再依赖外部 bzip2.exe 解包）──
# 2026-08-22: fresh clone 需能直接链接 STT，避免 CI/用户机缺 bzip2 导致 LNK2019。
# 覆盖上方全局 *.dll 规则。
!src-tauri/desktop/sherpa/
!src-tauri/desktop/sherpa/*.dll
!src-tauri/desktop/sherpa/*.lib
# npm 发布产物（大文件二进制，不提交 git）
npm-desktop/downloads/
npm-desktop/packages/nuphus-desktop-win32-x64/
npm-desktop/packages/nuphus-desktop-osx-arm64/
npm-desktop/packages/nuphus-desktop-linux-x64/
# npm 安装验证产物（一次性测试，不提交）
npm-desktop/install-test/
# 内部诊断/改造文档（不对外公开）
relay-device-routing.md
mobile-link-diagnosis.md

# 本地 AI 助手指引不入库
CLAUDE.md
# 独立 target 目录（cargo 检查/试验性构建产物，不入库）
target-check/
# dev 实例运行中无法写 target/debug（sherpa DLL 被占用），clippy/check 用这个目录绕开
target-clippy/

tsc-out.txt

# 审计/分析产出（不入库）
projects/

# 绕道编译/检查目录（cargo CARGO_TARGET_DIR 独立产物，不提交）
target-test/

# 本机专用的构建/启动脚本（内含本机绝对路径，不入库）
rebuild-and-run.bat
